com:
   sodbvi:
      name: sodbvi
      msg: 啊斯蒂芬阿德法撒旦法
      message: message is ${com.sodbvi.name}  ${com.sodbvi.msg}
spring:
   redis:
     database: 0
     host: 127.0.0.1
     port: 6379
     password:
     pool:
       max-active: 8
       max-wait: 60
       min-idle: 0
     timeout: 0
   session:
     store-type:
     redis:
       namespace: fuckredis
       flush-mode: on_save